// App code should import Prisma types from here (not @prisma/client) so the IDE
// always resolves the generated client under src/generated/prisma.
export { Prisma, PrismaClient } from '../generated/prisma/client.js';
